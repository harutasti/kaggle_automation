{
  "kaggle_competition_name": "titanic",
  "max_iterations": 2,
  "wca_per_iteration": 1,
  "stop_condition": {
    "score_threshold": 0.95,
    "no_improvement_iterations": 2
  },
  "experiments_base_dir": "./experiments",
  "log_file": "./logs/auto_kaggle.log",
  "log_level": "INFO",

  "_comment_execution_mode": "Options: 'simulation', 'codex'. Set to 'codex' to use real AI",
  "execution_mode": "codex",
  "simulation_mode": false,

  "_comment_codex": "Configuration for Codex AI execution",
  "codex": {
    "enabled": true,
    "timeout": 3600,
    "max_retries": 2,
    "track_usage": true,
    "skip_confirmation": true,
    "notes": "Codex CLI must be installed and in PATH"
  },

  "_comment_crawler": "Use real Kaggle competition data",
  "use_crawler": true,
  "max_discussions": 20,

  "_comment_safety": "Start with minimal settings for testing",
  "notes": [
    "This config is optimized for testing Codex integration",
    "Start with 1 agent and 2 iterations to avoid costs",
    "Use 'titanic' - small, fast competition for testing",
    "Once validated, increase wca_per_iteration and max_iterations"
  ]
}
